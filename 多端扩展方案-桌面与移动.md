# Papex 多端扩展方案：桌面科研工作平台 + 移动端 App

> 研究日期：2026-08-18
> 研究对象：将 papex（Next.js 16 + React 19 + Drizzle + PostgreSQL 学术文献平台）扩展为
> - 桌面端：Windows / Linux / macOS，定位「桌面科研工作平台」
> - 移动端：Android / HarmonyOS / iOS，定位「网页版替代品」（阅读论文、管理账号）

---

## 一、结论速览（TL;DR）

| 决策点 | 推荐方案 | 一句话理由 |
|---|---|---|
| 桌面端框架 | **Tauri 2.x**（Rust 内核 + 系统 WebView） | 安装包 5–15MB、内存 30–50MB，前端可复用 React/Tailwind，Rust 承担本地索引/PDF 解析等重活 |
| 移动端框架 | **React Native 0.77/0.82 + RNOH 鸿蒙适配** | 团队已是 React + TS 技术栈，RNOH 0.82.30 已正式发布、华为商城验证，三端（Android/iOS/鸿蒙）一套代码 |
| 桌面端前端 | 独立 **Vite + React + TS** 工程（从 Next.js 抽离客户端组件层） | Next.js 的 SSR/RSC 特性在桌面无用武之地，Vite 构建产物直接喂给 Tauri |
| 共享层 | `packages/shared`：API client + 类型 + i18n 字典 + 认证逻辑 | 从 OpenAPI 生成类型，三端共用 |
| 认证改造 | **新增 Refresh Token + 设备登录**，移动/桌面用原生安全存储 | 现有 JWT 存 httpOnly cookie 是浏览器方案，App 端需换 Bearer Token + 设备级撤销 |
| 上架渠道 | Windows: MSIX/NSIS 签名；macOS: 公证+App Store；Linux: AppImage/deb；Android: GP/商店；鸿蒙: AppGallery；iOS: App Store | 各平台签名/公证是上线硬门槛，需提前买证书 |

**核心判断**：papex 的 API 层已经相当完备（认证、论文、版本、PDF 流式、分类、引用、背书、语义检索、社区、管理全都有），多端复用的基础设施是现成的。主要工作集中在三块：① 桌面端重新组织前端（Vite + React 而非 Next.js）；② 认证体系改造（token 化 + 设备管理）；③ 桌面端补「科研工作平台」特有功能（本地 PDF 库、笔记、引用、离线索引）。

---

## 二、现有项目多端复用基础评估

对 papex 现状的探查结论（详见 `src/app/api/` 与 `src/lib/`）：

### 2.1 可直接复用的资产（利好）

- **API 几乎全覆盖**：认证（login/register/logout/me）、论文列表/详情/版本、PDF 流式下载、分类（257 条双语）、引用、背书、收藏、订阅、站内信、工单、协审、管理员、语义检索（pgvector + HNSW）、健康检查、**OpenAPI 文档**（`/api/openapi.json`）。
- **已有 API Key 机制**：`pk_live_...` 格式，`Authorization: Bearer` 认证，分 read/write scope，继承用户 RBAC —— 这是 App 端现成的认证通道。
- **数据模型成熟**：papers / paper_versions（含 pdfUrl、doi、sourceUrl、license、embedding）/ categories（name_zh 双语）/ citations / endorsements / bookmarks / subscriptions / messages / tickets / co_reviews 均已落地。
- **已有 LaTeX 写作包**（`papex-latex/`）：桌面端「写作集成」功能有基础。

### 2.2 需要改造的短板（关键适配点）

| 问题 | 现状 | 影响 |
|---|---|---|
| 认证绑定浏览器 | JWT 存 httpOnly cookie `papex_session`，TTL 7 天，**无 refresh token** | App 端无法依赖 cookie，需 token 化 |
| 无统一 API 客户端 | 前端各处直接 `fetch("/api/...")`，无错误归一化封装 | 三端需各自实现，应抽共享层 |
| API 返回裸 JSON | `{error: "中文消息"}` + 状态码，无 `{ok,data,error}` 包装 | 客户端需自行归一化错误 |
| Next.js 前端不可直接迁移 | 组件大量用 RSC/Server Actions，依赖 `next/link`、`next/font` 等 | 桌面/移动端需独立前端工程 |
| 无笔记/批注数据模型 | 论文有 comments 但无「个人阅读笔记/PDF 批注」概念 | 桌面科研工作平台的核心功能需新建表 |
| 无离线能力 | 全部在线请求 | 桌面/移动都要求本地缓存 |

---

## 三、需求分析

### 3.1 桌面端：科研工作平台（Win/Linux/macOS）

对标 Zotero + 主流 PDF 阅读器，目标用户是深度研究者。核心场景：

1. **文献库管理**：三栏布局（分类树 / 文献列表 / 详情面板），收藏、标签、保存的搜索、智能筛选（按分类/引用数/时间/语义相似度）
2. **PDF 深度阅读**：内嵌阅读器，高亮、批注、划词笔记、阅读进度记忆
3. **引用管理**：生成参考文献（CSL 风格）、导出 BibTeX，与 LaTeX/Word 写作集成（复用 papex-latex）
4. **引文图谱**：把网页版的 citation-graph 升级为桌面交互式视图（已有基础）
5. **语义检索**：调用服务器 embedding 检索；可扩展本地索引（标题/摘要全文）
6. **离线与同步**：本地缓存 PDF 全文，后台静默同步收藏/笔记/订阅
7. **追更与提醒**：订阅分类/作者，系统通知新论文
8. **平台整合**：全局快捷键、系统托盘、深色模式跟随系统、原生菜单/拖放/文件关联

### 3.2 移动端：网页版替代品（Android/HarmonyOS/iOS）

定位轻量、移动场景优先：

1. **账号体系**：登录/注册、多账号切换、设备管理（远程撤销登录）、API Key 管理
2. **论文阅读**：检索（关键词+语义）、分类浏览、**PDF 离线下载与阅读**（进度记忆、基础标注）、深色模式
3. **个人中心**：我的收藏、我的订阅、我的背书、收到的背书、消息中心（站内信/工单）
4. **轻量社区**：提交反馈、工单、协审提醒
5. **多语言**：zh/en 跟随网页端

---

## 四、技术栈选型：桌面端

### 4.1 候选对比（2026-08 现状）

| 维度 | Tauri 2.x | Electron 34.x | Flutter Desktop | Wails 3 |
|---|---|---|---|---|
| 后端语言 | Rust | Node.js | Dart | Go |
| 安装包体积 | **5–15 MB** | 80–200 MB | ~20–40 MB | 10–25 MB |
| 空闲内存 | **30–50 MB** | 120–400 MB | 中等 | 低 |
| 渲染内核 | 系统 WebView（Win WebView2 / mac WKWebView / Linux WebKitGTK） | 内置 Chromium（三端像素级一致） | 自绘 Skia/Impeller（三端一致） | 系统 WebView |
| 移动端 | v2 支持 iOS/Android（不支持鸿蒙） | 不支持 | 支持，但鸿蒙需切定制版 | 不支持 |
| 生态 | 官方 30+ 插件，增长快（GitHub 年增 55%） | **最成熟**（VS Code/Slack/Discord/Figma 验证） | 移动生态强、桌面社区小 | 较小 |
| 自动更新 | 官方 updater（全量包，需自建托管+签名） | electron-updater（差分+分阶段+签名公证，最成熟） | 需三方 | 三方 |
| 学习成本 | 需少量 Rust | 纯 JS | 需 Dart | 需 Go |
| 风险点 | 三端 WebView 渲染差异；Linux WebKitGTK 性能怪癖 | 体积大、常驻内存高 | 桌面生态弱；鸿蒙版与上游版本错位 | 不适合本团队 |

### 4.2 推荐：Tauri 2.x

理由（针对本项目）：

1. **「科研工作平台」是常驻型应用**：用户可能开着一整天，内存/启动速度直接体感相关。Tauri 的空闲内存约为 Electron 的 1/4，冷启动 <0.5s。
2. **前端复用度最大化**：Tauri 只关心构建后的 Web 静态资源，React + Tailwind + shadcn 风格组件可直接搬。团队零新增前端语言。
3. **Rust 干重活**：本地 PDF 解析、全文索引（如 tantivy）、文件哈希去重、后台同步队列——这些正是 Rust 的舒适区，且能提升平台级体验（原生菜单、托盘、文件关联、全局快捷键）。
4. **安全性默认更好**：capability 白名单机制，默认全禁、按需授权，适合处理用户本地科研资料这种敏感数据。
5. **Tauri v2 已是稳定 GA 版本**（2.x 线，2026 年生态成熟度足够，字节等大厂已量产）。

**什么时候该选 Electron**（备选）：如果未来桌面端要做「完全一致的像素渲染」（如复杂图表/公式渲染三端必须一模一样）、或深度依赖 Node 生态的原生模块，或团队完全拒绝 Rust。对 papex 而言（图表用 ECharts、公式用 LaTeX 渲染），Tauri 的系统 WebView 在现代系统上表现已足够，选 Tauri 收益更大。

### 4.3 桌面端前端组织方式

**不要**直接拿 Next.js 工程打进 Tauri（SSR、RSC、中间件、`next/link` 全是浏览器/服务端概念，桌面端用不上且 webview 加载远程 SSR 页面与「本地应用」定位矛盾）。

建议新建独立前端工程 `apps/desktop`：

```
apps/desktop
├── src/                # Vite + React + TS + Tailwind 4
│   ├── pages/          # 文献库 / 阅读器 / 引用 / 设置 / 登录
│   ├── components/     # 复用自网页端客户端组件（按需移植）
│   └── lib/            # Tauri invoke 封装、API client
├── src-tauri/          # Rust：PDF 解析、本地索引、托盘、更新器、系统集成
└── package.json
```

组件复用策略：网页端 `src/components/` 里**纯客户端组件**（卡片、列表、筛选栏、图表）按需拷贝/提取到共享包；依赖 RSC/Server Action 的页面逻辑全部重写为 API 调用。

---

## 五、技术栈选型：移动端

### 5.1 候选对比（2026-08 现状）

| 维度 | React Native + RNOH | Flutter（Flutter-OH 定制版） | uni-app x | 原生 ArkTS |
|---|---|---|---|---|
| 语言 | TS/React（**与现有栈一致**） | Dart（需学新语言） | Vue + UTS（需学 Vue，且与 RN 前端不互通） | ArkTS（只服务鸿蒙） |
| Android/iOS | 官方一级支持 | 官方一级支持 | 支持 | 不支持 |
| HarmonyOS | **RNOH 0.82.30 已正式发布**（2026-05），0.72.90+ LTS 生产验证，华为商城改造案例 | 上游官方**无鸿蒙支持**；社区定制版 Flutter-OH 3.35.7（2026-06 迁至 CPF-Flutter），版本滞后于官方（官方 3.44 vs 鸿蒙 3.35.7） | 支持（HBuilderX 4.24+，uni-app x 5.0），华为合作 | 官方原生，性能最佳 |
| 鸿蒙成熟度 | 高（社区活跃、版本同步 4 个/年） | 中（SIG 成立 2026-01，同步周期 7→4 个月，仍在追赶） | 中高（DCloud 深耕国内） | 最高 |
| PDF 阅读生态 | react-native-pdf 等成熟 | 成熟 | 中 | 一般 |
| 与桌面端技术共享 | **与 Tauri 桌面端共享 TS API client** | 与 Flutter 桌面共享 | 仅与 Vue 系共享 | 无 |
| 团队成本 | 最低（React 知识直接迁移） | 中（新语言+新思维） | 中（Vue 思维） | 高（单独维护鸿蒙端） |

### 5.2 推荐：React Native 0.77/0.82 + RNOH

理由：

1. **技术栈零鸿沟**：现有团队全栈是 React + TS。RN 组件模型、Hooks、状态管理（Zustand 都能用）与网页端一脉相承；TS 类型、API client、i18n 字典可三端共享。
2. **鸿蒙是硬需求，RNOH 是目前最成熟的三端路线**：
   - RNOH 0.82.30 于 2026-05 正式发布（New Architecture 唯一架构、Hermes 优化、React 19.1.1）；
   - 0.72.90+ LTS 长期维护线是生产首选；0.77.10+ 支持鸿蒙 PC/平板；
   - 华为商城 App 已用 RNOH 改造并上线，性能数据公开（首页加载 1.2s→780ms）。
   - 2026 年官方路线图一年适配 4 个上游版本（0.82→0.84→0.86→0.88）。
3. **Android + iOS + 鸿蒙三端一套代码**，与 Tauri 桌面端共享同一份 TS API client，整个项目只有「Tauri(Rust) + RN(TS)」两套构建体系，没有第三种语言。
4. **PDF 阅读**：`react-native-pdf` 生态成熟，支持本地文件与 URL 流式渲染。

**什么时候选别的**：
- 若更看重**渲染一致性与性能极致**且不排斥新语言 → Flutter（但鸿蒙必须锁定制版 SDK，版本会一直比官方滞后，且三方插件需逐个核对鸿蒙适配，风险高于 RNOH）。
- 若主要面向**国内小程序生态**（微信/支付宝/抖音）且接受 Vue → uni-app x。
- 若鸿蒙是唯一目标 → 原生 ArkTS。

### 5.3 关于「是否直接用 WebView 套壳（PWA/Capacitor）」

不推荐作为主方案。理由：① 移动端要**离线阅读 PDF + 原生存储 token**，WebView 方案在这些点体验差、且受限于 WebView 内核版本；② 「网页版替代品」的定位要求接近原生的启动与滚动体验，RN 成本并不高。PWA 可以作为**低成本验证**的第一步行进方案（见路线图 Phase 0）。

---

## 六、总体架构

```
                        ┌─────────────────────────────┐
                        │        papex 服务器          │
                        │  Next.js API + Drizzle + PG │
                        │  (pgvector 语义检索 / PDF 存储)│
                        └──────────────┬──────────────┘
                             HTTP(S) + Bearer Token
              ┌────────────────────────┼────────────────────────┐
              │                        │                        │
   ┌──────────▼─────────┐   ┌──────────▼─────────┐   ┌──────────▼─────────┐
   │  Desktop (Tauri 2) │   │   Mobile (RN)      │   │    Web (现有)       │
   │ Win/Linux/macOS    │   │ Android/iOS/鸿蒙    │   │   浏览器            │
   │                    │   │                    │   │                    │
   │ Vite+React UI      │   │ RN UI              │   │ Next.js UI         │
   │ Rust 本地能力:      │   │ 原生存储 token      │   │ httpOnly cookie    │
   │  PDF解析/全文索引    │   │ 离线PDF缓存         │   │                    │
   │  托盘/快捷键/更新    │   │ 推送通知           │   │                    │
   └────────────────────┘   └────────────────────┘   └────────────────────┘
              │                        │
              └──────────┬─────────────┘
                    ┌────▼────┐
                    │ shared  │  API client / 类型(OpenAPI生成) / i18n / 认证
                    └─────────┘
```

### 6.1 共享层设计（monorepo 改造）

将仓库调整为 pnpm workspaces（或保留现有结构 + `packages/`）：

```
papex/
├── apps/
│   ├── web/            # 现有 Next.js（迁移后）
│   ├── desktop/        # Vite + React + Tauri
│   └── mobile/         # React Native + RNOH
├── packages/
│   ├── shared/         # TS：api client、类型、错误归一化、i18n 字典、认证工具
│   └── ui/             # 跨端纯 UI 组件（可选，卡片/列表/筛选）
```

- `packages/shared` 的类型直接由 `openapi.json` 生成（`openapi-typescript`），一次生成三端共用，杜绝手写类型漂移。
- API client 统一封装：base URL 注入、Bearer token 注入、错误归一化（401 自动刷新）、重试、超时。

### 6.2 认证改造（App 端关键路径）

现状：JWT 存 httpOnly cookie，无 refresh token。App 端方案：

1. **新增 token 化认证**：
   - `POST /api/auth/login` 在原有 cookie 之外，返回 `{ accessToken, refreshToken }`（新接口或扩展现有）；
   - Access Token：短 TTL（如 15–60 分钟），`Authorization: Bearer` 携带；
   - Refresh Token：长 TTL（如 30 天）+ **轮换**（每次刷新换新）+ **设备绑定**（存设备表，可撤销）。
2. **新增设备管理**：`devices` 表（设备名、平台、最后活跃、refresh token hash），支持 `/me/devices` 列表与远程撤销——对应移动端「管理账号」需求，也是安全底线。
3. **存储**：
   - iOS：Keychain；Android：EncryptedSharedPreferences/Keystore；鸿蒙：HUKS（华为统一密钥库）；
   - 桌面：系统钥匙串（macOS Keychain / Windows Credential Manager / Linux Secret Service），Tauri 有现成插件（`tauri-plugin-keyring` 生态）或 Rust `keyring` crate。
4. **现有 API Key 通道保留**：给进阶用户/命令行场景用（`pk_live_...`）。

### 6.3 需要后端补齐的 API（工作量清单）

| 接口 | 说明 | 优先级 |
|---|---|---|
| `POST /api/auth/refresh` + 设备表 | token 轮换、撤销 | P0 |
| `/api/me/devices` | 设备列表/远程登出 | P0 |
| 笔记/批注 CRUD（`notes` 表，挂 paper_version + 页/坐标/文本高亮） | 桌面阅读器 + 移动标注的云端同步 | P0 |
| 阅读进度同步（`reading_progress` 表） | 跨设备续读 | P1 |
| 离线包接口（批量导出选定论文 PDF + 元数据，zip 或清单） | 移动端离线阅读、桌面端初始化同步 | P1 |
| 订阅推送（Web Push / APNs / FCM / 鸿蒙推送） | 新论文提醒 | P2 |
| OpenAPI 文档完善 + 版本化 | 多端契约的单一事实来源 | P0 |

### 6.4 桌面端本地技术选型细节

| 能力 | 方案 |
|---|---|
| PDF 渲染/标注 | 前端 `pdf.js`（渲染 + 高亮层自定义实现）；或集成成熟的 `pdfium`（Rust）做本地解析 |
| 全文索引（可选增强） | Rust `tantivy`（本地倒排索引，离线全文检索） |
| 笔记/标注存储 | 本地 SQLite（Rust `rusqlite` 或 Tauri SQL 插件）+ 云端 notes 表双向同步 |
| 自动更新 | Tauri updater（自建更新服务器/放 GitHub Releases，需代码签名：Windows 证书 + macOS 公证） |
| 托盘/快捷键/文件关联 | Tauri 内置 API + 官方插件 |
| 深色模式 | 跟随系统 + 手动覆盖（复用 next-themes 逻辑，Tauri 可监听系统主题） |

---

## 七、功能模块全景（桌面端 MVP → 完整版）

### 桌面端功能模块（分三档落地）

**MVP（V1）**：
- 登录/多账号切换（token 化）
- 文献库三栏布局：分类树（257 分类）/ 列表（筛选/排序/收藏/标签）/ 详情面板
- PDF 内嵌阅读器：翻页、缩放、搜索、阅读进度记忆
- 基础标注：高亮 + 文本笔记（云端同步）
- 语义检索 + 关键词检索（复用现有 API）
- 离线缓存已读 PDF
- 引文图谱查看（复用现有数据）

**V2（科研工作平台成型）**：
- 引用管理：CSL 样式生成参考文献、导出 BibTeX/EndNote、复制引用
- 写作集成：对接 papex-latex 包（LaTeX 引用插入）
- 保存的搜索 / 智能筛选 / 标签批量管理
- 订阅分类与作者 + 系统通知
- 阅读统计（时间、论文数、分类分布）
- 托盘 + 全局快捷键

**V3（进阶）**：
- 本地全文索引（tantivy）+ 离线语义检索（可选：本地 embedding 模型）
- PDF 批量导入（拖放上传到服务器或仅本地库）
- 团队/协作视图（协审、背书、评论聚合）
- 插件系统（借鉴 Zotero 思路，为社区扩展留口）

### 移动端功能模块（MVP → V2）

**MVP（V1）**：
- 登录/注册/登出、多账号切换、设备管理（撤销登录）
- 首页：最新论文、分类浏览、关键词/语义检索
- 论文详情：元数据、引用、PDF 查看（在线流式）
- 收藏/订阅/消息中心
- 个人主页：背书查看
- 深色模式、zh/en

**V2**：
- PDF 离线下载与离线阅读（进度/标注同步）
- 推送通知（新论文、工单回复）
- 工单/反馈/协审入口
- 扫码登录网页版（可选，调通 token 后可做）
- API Key 管理入口

---

## 八、分阶段路线图

| 阶段 | 内容 | 预估工作量（人周） |
|---|---|---|
| **Phase 0：地基**（先做） | monorepo 改造；`packages/shared`（API client + OpenAPI 类型生成）；认证改造（refresh token + devices 表 + 各端安全存储）；后端补齐笔记/进度/离线包接口；PWA 壳快速验证移动端 UI 与 API 契约 | 4–6 |
| **Phase 1：移动端 MVP** | RN 工程初始化（含 RNOH 鸿蒙配置）；登录/浏览/检索/阅读/收藏/个人中心；双端发布（Android + iOS）；鸿蒙跟随验证 | 6–8 |
| **Phase 2：桌面端 MVP** | Tauri 2 工程；文献库三栏 + PDF 阅读器 + 标注同步 + 离线缓存；三平台打包（win/linux/mac）+ 签名公证 + 自动更新 | 8–10 |
| **Phase 3：桌面科研工作平台化** | 引用管理/写作集成/保存搜索/通知/统计；本地全文索引 | 6–8 |
| **Phase 4：移动端 V2 + 鸿蒙正式上架** | 离线 PDF/标注、推送、AppGallery 上架、全量发布 | 4–6 |

> 合计约 28–38 人周（约 2 人全职 4–5 个月）。若先只做移动端 MVP + 桌面 MVP（P0+P1+P2），约 18–24 人周。

---

## 九、风险与注意事项

1. **鸿蒙生态风险**：RNOH 虽然成熟，但三方库需逐个核对鸿蒙适配清单（版本矩阵：0.72.90+ 最稳、0.77.10+ 新项目推荐、0.82.30 最新）。建议 Phase 1 就把鸿蒙真机跑通作为验收项，别拖到最后。
2. **跨 WebView 渲染差异（Tauri）**：Win WebView2 / macOS WKWebView / Linux WebKitGTK 行为有差异，建议 CSS 特性使用保守子集，并在三平台分别做视觉回归。
3. **上架合规是硬门槛**：Windows 代码签名证书、macOS Developer ID 公证、iOS 开发者账号、AppGallery 实名与审核；Linux 无门槛但需要维护 AppImage/deb 产物。这些要提前 2–4 周启动。
4. **Next.js 组件复用边界**：RSC/Server Action 组件不能直接迁移，需按「纯客户端组件」清单逐个提取，避免高估复用率。
5. **中文错误消息**：API 错误 `{error: "中文"}` 需要客户端 i18n 化（App 内按 locale 显示或映射错误码），建议后续在 API 层加错误码字段。
6. **数据同步冲突**：笔记/标注/进度一旦支持离线编辑，需要简单的冲突策略（last-write-wins + 版本号即可，MVP 不必上 CRDT）。
7. **PDF 标注的持久化格式**：建议自定 JSON 格式（页、四角坐标、颜色、文本），前端渲染层与存储解耦，避免绑定某个库。
8. **隐私**：桌面端涉及用户本地文件（PDF 导入、索引），需在隐私说明与首次启动引导中明确「本地数据不上传」的边界。

---

## 十、备选方案（若想更激进/更保守）

- **保守（推荐先验证）**：Phase 0 用 **PWA** 验证移动端（iOS/Android 均可添加到主屏幕，鸿蒙浏览器也支持），确认 API 契约与 UI 方向后再投入 RN——PWA 与 RN 的 API client 可以共享，投入不浪费。
- **激进（一套代码全部覆盖）**：全 Flutter（桌面 + Android/iOS + 鸿蒙定制版）——语言统一是最大诱惑，但代价是：团队学 Dart、鸿蒙锁定制 SDK 版本（官方 3.44 vs 鸿蒙 3.35.7 错位）、前端组件无法复用现有 React 资产，且桌面端生态弱于 Tauri。
- **桌面端完全放弃自研前端**：直接用 Tauri 加载现有网页版 URL（`tauri-plugin-http` + WebView 指向线上站点）——零前端成本，但离线、本地索引、托盘、原生菜单等「平台感」功能全部缺失，仅适合内部快速验证。

---

*附录：调研数据来源（2026-08 检索）*
- Tauri/Electron 对比：dev.to / ByteLedger / CSDN 2026 年多篇评测（安装包 2–10MB vs 80–200MB，内存 30–50MB vs 120–400MB，Tauri v2 GA、GitHub 年增 55%）
- RNOH：华为开发者社区 RNOH 2026 路线图 + 51CTO 版本矩阵（0.72.90+ LTS / 0.77.10+ / 0.82.30 正式版 2026-05 / 0.84 预览）+ 华为商城改造公开数据
- Flutter 鸿蒙：OpenHarmony Flutter SIG（2026-01 成立）、CPF-Flutter 组织迁移（2026-06）、Flutter-OH 3.35.7；上游官方无鸿蒙支持
- uni-app：uni-app 官网 / uni-app x 文档（鸿蒙自 HBuilderX 4.24+ 支持，蒸汽模式原生渲染）
- Zotero 功能范式：zotero.org 官方功能页
